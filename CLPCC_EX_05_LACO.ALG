Algoritmo "clpcc_ex_05_laco"

Var
   a, b, c, d : inteiro
   soma : real


Inicio
      escreval("Digite 4 numeros: ")
      leia(a, b, c, d)
      
      soma <- (a^2) + (b^2) + (c^2) + (d^2)

       escreval("soma: ", soma)
       
Fimalgoritmo