algoritmo "clpcc_laco"

var
   i : inteiro

inicio
      i <- 1
      enquanto (i <= 100) faca
      escreval(i)
              i <- i + 1
      fimenquanto
      
      escreval("Depois do laço")

fimalgoritmo