algoritmo "clpcc_ex11_mc"

var

   c, f : real
     
inicio
      escreva("Qual a temperatura em celsius? ")
      leia(c)
      
      f <- (9 * c + 160) / 5
      escreval("A temperatura em fahreinheit: ", f)

fimalgoritmo